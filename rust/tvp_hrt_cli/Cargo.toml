[package]
name = "tvp_hrt_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde_json = "1"

# workspace crates
tvp_hrt_engine = { path = "../tvp_hrt_engine" }
serde = { version = "1", features = ["derive"] }
